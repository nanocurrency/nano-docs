name: cleanup-docker
on:
  pull_request:
    types: [closed]

env:
  PR_NUMBER: ${{ github.event.number }}
  OWNER: orgs/nanocurrency
  PACKAGE_NAME: nano-docs
  PER_PAGE: 100

jobs:
  container_cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Clean up PR specific container and any untagged containers
        uses: actions/github-script@v3
        with:
          github-token: ${{ secrets.GHCR_TOKEN }}
          script: |
            const response = await github.request("GET /${{ env.OWNER }}/packages/container/${{ env.PACKAGE_NAME }}/versions",
              { per_page: ${{ env.PER_PAGE }}
            });
            for(metadata of response.data) {
                if (metadata.container.tags.length == 0 || metadata.container.tags == "${{ env.PR_NUMBER }}") {
                    console.log("delete " + version.id)
                    const deleteResponse = await github.request("DELETE /${{ env.OWNER }}/packages/container/${{ env.PACKAGE_NAME }}/versions/" + version.id, { });
                    console.log("status " + deleteResponse.status)
                }
            }
          